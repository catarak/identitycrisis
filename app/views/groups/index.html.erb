<div class="row">
  <div class="col-md-6">
    <h3>Groups</h3>
    <ul>
    <% @groups.each do |group| %>
      <li><%= link_to "#{group.name}", group_path(group) %></li>
    <% end %>
    </ul>
  </div>
</div>